const initialState = {
  // adding payload to initialState
  ping: {
    payload: {},
    isLoading: false,
    isSuccess: false,
    isError: false,
    error: null,
  },
}

export default initialState
